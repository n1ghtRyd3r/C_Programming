// Incluimos las librerías necesarias para poder ejecutar el archivo 

#include <stdio.h>

// Todos los programas en C tienen funciones (int main) pero esta es la inicial.
// indicamos el inicio de nuestro programa
int main()
{
    // Indicamos el contenido del programa y al final indicamos un ";" para indicar 
    // el final de la línea y "\n" para indicar un salto de línea
    printf("Hola mundo \n");

    // Indicamos que el programa ha terminado exitosamente
    return 0;
};